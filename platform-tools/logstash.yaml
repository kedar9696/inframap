- defaultTab: nodes
  description: Install or Uninstall LOGSTASH online or offline
  executionEnabled: true
  group: platform-tools
  id: 7e647e64-ffee-4cd3-a102-016fe8a972eb
  loglevel: INFO
  name: logstash
  nodeFilterEditable: false
  nodefilters:
    dispatch:
      excludePrecedence: true
      keepgoing: false
      rankOrder: ascending
      successOnEmptyNodeFilter: false
      threadcount: '1'
    filter: 'tags: logstash_hosts'
  nodesSelectedByDefault: true
  notification:
    onfailure:
      email:
        attachLog: true
        attachLogInFile: true
        recipients: ${globals.rundeck-email-notif}
    onsuccess:
      email:
        attachLog: true
        attachLogInFile: true
        recipients: ${globals.rundeck-email-notif}
  notifyAvgDurationThreshold: null
  options:
  - enforced: true
    label: Action
    name: Action
    required: true
    sortValues: true
    valuesUrl: file:${globals.gra-playbooks-path}/rundeck/options/action.json
  - description: Installation Type
    enforced: true
    label: InstallationType
    name: InstallationType
    required: true
    sortValues: true
    valuesUrl: file:${globals.gra-playbooks-path}/rundeck/options/installationType.json
  - description: ConfigureFirewall Option
    enforced: true
    label: configureFirewall
    name: configureFirewall
    required: true
    value: 'true'
    valuesUrl: file:${globals.gra-playbooks-path}/rundeck/options/configureFirewall.json
  - description: Version
    enforced: true
    label: Version
    name: version
    required: true
    valuesUrl: file:${globals.gra-playbooks-path}/rundeck/options/tools-logstash.json
  plugins:
    ExecutionLifecycle: null
  scheduleEnabled: true
  sequence:
    commands:
    - configuration:
        ansible-base-dir-path: ${globals.gra-playbooks-path}
        ansible-become: 'false'
        ansible-disable-limit: 'false'
        ansible-extra-vars: "operation: ${option.Action} \nmode: ${option.InstallationType}\
          \ \nartifact_build_ver: ${option.version}\nconfigureFirewall: ${option.configureFirewall}"
        ansible-playbook: playbook-logstash.yml
        ansible-ssh-passphrase-option: option.password
        ansible-ssh-use-agent: 'false'
      nodeStep: false
      type: com.batix.rundeck.plugins.AnsiblePlaybookWorkflowStep
    keepgoing: false
    strategy: node-first
  uuid: 7e647e64-ffee-4cd3-a102-016fe8a972eb

